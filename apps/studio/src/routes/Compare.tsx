/** Placeholder — F4 adds the 2–4 design picker, overlaid temperature curves and KPI delta table. */
export function Compare() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16 sm:px-6">
      <h1 className="text-xl font-semibold">Compare</h1>
      <p className="mt-2 max-w-md text-sm text-ink-muted">
        Pick 2–4 saved designs to overlay their temperature curves and compare KPIs side by side.
      </p>
    </div>
  );
}
