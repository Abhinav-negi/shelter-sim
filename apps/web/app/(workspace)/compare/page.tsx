'use client';

// apps/web/app/(workspace)/compare/page.tsx
//
// Stub only -- the user said "possibly" a compare page, and there's no
// existing comparison component to wire in (`scenarios`/`ScenarioResult`
// exists on the store but nothing renders it as a table yet; that's new
// product surface, not part of this redesign).

export default function ComparePage() {
  return (
    <div className="page">
      <h1>Compare</h1>
      <p className="placeholder">Compare view — coming soon.</p>
    </div>
  );
}
