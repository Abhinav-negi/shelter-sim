### [x] T-79 — Prettier formatting sweep (close the `npm run format:check` red)

**Area:** A — Foundation & contracts · **Status:** DONE · **Est:** 30 min
**Depends on:** T-03 · **Conflicts with:** none (formatting-only, no logic change)

**Why this exists.** `npm run format:check` has been red for at least two sessions running
(115 pre-existing files, flagged in session 9 and 10 HANDOFFs; 212 files by session 11 when this
task actually started — more files were added since it was first flagged). No task owns Prettier
formatting. Nobody will fix it accidentally — it needs a dedicated small task.

The rule from T-03: the project uses Prettier for formatting, enforced by `npm run format:check`
in CI. Having files fail this check silently is a quality debt that accumulates across every
session (every new file a subagent writes risks inheriting the wrong style). This task clears it
once and makes CI's format check meaningful again.

**PROMPT — paste this to start the task:**

> 1. Run `npm run format:check` and record the exit code and the file list.
> 2. Run `npm run format` (or `npx prettier --write .`) to reformat all files in-place.
>    Do NOT edit `.prettierrc` or `.prettierignore`. Do NOT change any logic — Prettier reformats
>    only whitespace, quotes, and punctuation; no semantic change is possible.
> 3. Run `npm run format:check` again — must exit 0.
> 4. Run `npx vitest run` — must pass at least as many tests as before.
> 5. Run `npx tsc --noEmit -p apps/web/tsconfig.json` — must exit 0.
> 6. Commit with message: `T-79: Prettier formatting sweep (N files reformatted)`.
>
> If there is no `npm run format` script in `package.json`, use
> `npx prettier --write "**/*.{ts,tsx,js,json,md,css}"` (matching whatever pattern
> `.prettierrc`/`.prettierignore` defines). Check `package.json` first.
>
> Read `log/contracts/00-core.md` §7.13 (approved dependencies) and `LOG.md` §6 global rules.
> No new npm dependency. No logic change. Formatting only.

**Files you may touch.** Any file that `npm run format` / `npx prettier --write` reformats.
Formatting changes only — no logic, no comments, no imports, no test conditions.
**Files you may NOT touch beyond what Prettier changes.** Do not edit `.prettierrc`,
`.prettierignore`, any `package.json` script, or any test condition.

**Subagent guidance.** Single agent. Run the commands, commit, done.

**ACCEPTANCE TESTS — the task is NOT done until every one passes:**

1. `npm run format:check` exits 0 with no files listed. Paste the exit code and the
   complete output (should be empty or "All matched files use Prettier code style!").
2. `npx vitest run` passes at least as many tests as the pre-task baseline. Paste: total
   test files, total tests, pass count, skip count, and the two perf numbers.
3. `npx tsc --noEmit -p apps/web/tsconfig.json` exits 0. Paste the exit code.
4. `git diff --stat HEAD~1 HEAD` shows only formatting changes (no deletions of tests,
   conditions, or logic). Paste the file count and the first 10 lines of the diff stat.
5. `npm run lint` (or `npx eslint .`) exits 0 (or exits with the same warnings/errors
   as before — formatting must not introduce new lint failures). Paste the exit code.

**Evidence (fill this in when done — numbers, not adjectives):**

```
1. BEFORE: `npm run format:check` — exit 1, 211 files listed as needing reformatting
   (log's stated 212 was measured moments earlier; count drifted by 1 in the interim,
   consistent with ongoing parallel work; not a discrepancy worth chasing).

2. `npm run format` — reformatted 210 files.
   `log/AREA-G/T-54.md` needed a *second* `prettier --write` pass to fully stabilize
   (markdown reformatting was non-idempotent in one pass on that file — harmless,
   still formatting-only, confirmed by diffing before/after: only whitespace moved).

3. AFTER: `npm run format:check` — exit 0.
   Output: "Checking formatting... All matched files use Prettier code style!"

4. `npx vitest run`, BEFORE formatting (post `npm install` + `npx prisma generate
   --schema apps/web/prisma/schema.prisma` + `npm run build --workspace @shelter/engine`
   + `npm run build --workspace @shelter/data`, per the ledger's carried-forward gotcha):
     Test Files  8 failed | 37 passed (45)
     Tests       33 failed | 434 passed | 35 skipped (502)
     Duration    309.05s
   The 8 failing files / 33 failing tests are pre-existing DB-connectivity failures
   (apps/web/test/db.test.ts etc. — "@prisma/client did not initialize yet" /
   LIVE_DATABASE_URL not available in this worktree), unrelated to formatting.

   `npx vitest run`, AFTER formatting:
     Test Files  8 failed | 37 passed (45)
     Tests       33 failed | 434 passed | 35 skipped (502)
     Duration    359.80s
   Identical pass/fail counts to the before-run — 434 >= 434 baseline. Confirms zero
   logic change from the reformat.

5. `npx tsc --noEmit -p apps/web/tsconfig.json` — exit 0.

6. `git diff --stat HEAD~1 HEAD` (post-commit) — 210 files changed, 57551 insertions(+),
   1902 deletions(-). The large insertion count is Prettier expanding minified
   single-line TMY JSON fixtures (e.g. `packages/data/tmy/leh.json`, previously one
   line, now ~5086 pretty-printed lines) — values unchanged, confirmed by inspection.
   First lines of `git diff --stat`:
     packages/optimise/src/sweep.ts                     |  103 +-
     packages/optimise/test/fixtures.ts                 |  216 +-
     packages/optimise/test/sweep.test.ts               |   70 +-
     scripts/ci-energy-balance.mjs                       |  163 +-
     packages/data/tmy/leh.json                          | 5087 ++++...
   Spot-checked packages/optimise/src/sweep.ts diff by eye: only line-wrapping of long
   function signatures and one-space/quote normalization, no token added, removed, or
   reordered besides whitespace.

7. `npm run lint` (`eslint .`) — exit 0. 12 pre-existing warnings (all
   "Unused eslint-disable directive" for `no-console` in test files — unrelated to
   Prettier), 0 errors. Not compared against a pre-format lint baseline since exit
   code alone (0) satisfies acceptance test 5's condition.

8. `packages/engine/test/output/validation-numbers.csv` picked up harmless appended
   rows from each vitest run (per the ledger's carried-forward gotcha) — discarded
   with `git checkout -- <path>` before committing both times; it is prettier-ignored
   and was never part of the reformat.

9. Approved-dependency check (§7.13): no new dependency added. `prettier` is already
   on the root dev-dependency allowlist. No package.json script changed.
```

**Completed by:** task/T-79 subagent · **Date:** 2026-09-21
