### [x] T-75 — Stack T-53's `<DayNightAnimation>` behind `<HouseView>` in app-shell.tsx's `slot-house`

**Area:** F — Frontend (new task, raised by session 9's own HANDOFF — the top-priority follow-up
named once T-74 closed the prior i18n gap) · **Status:** CLAIMED by orchestrator (session 10) at 2026-09-21T00:50:23Z · **Est:** 2 h
**Depends on:** T-53, T-71 · **Conflicts with:** none

**Why this exists.** T-53 built `apps/web/components/house/daynight/DayNightAnimation.tsx` in full
— sun, sky, shadow, stars, the progress ring, all driven by the real `sunPosition` from
`@shelter/engine` — but its own allow-list forbade touching `app/app-shell.tsx`, so `slot-house`
there still renders only `<HouseView />` (from T-71). T-53's own Evidence block documents the exact
resolution needed and why: `DayNightAnimation`'s root element is `position: absolute; inset: 0`
(`DayNightAnimation.tsx` line ~189), so it renders nothing visible without a `position: relative`
ancestor, and it reproduces `HouseView.tsx`'s own `viewBox` formula verbatim
(`boundingBox(building)`, `pad = 1`) specifically so that stacking the two under one
`position: relative` wrapper — animation first (painted behind), `HouseView` second (painted on
top) — lands the shadow polygon exactly on `HouseView`'s own house silhouette, pixel for pixel. This
task is pure integration, the same shape as T-71/T-73: no new physics, no new design decision, just
rendering an already-built, already-tested component in the one spot it was built to occupy.

**PROMPT — paste this to start the task:**

> In `apps/web/app/app-shell.tsx` only, inside `<main className="col" aria-label="house and
views">`, replace the current bare `<HouseView />` with:
>
> ```tsx
> <div style={{ position: 'relative' }}>
>   <DayNightAnimation />
>   <HouseView />
> </div>
> ```
>
> `DayNightAnimation` in that order — **before** `HouseView` in JSX/DOM order, not after — so it
> paints behind (its own root is `position: absolute; inset: 0` and has `pointerEvents: 'none'`, so
> it never blocks clicks on `HouseView`'s walls). Import it from
> `../components/house/daynight` (the barrel). Zero props — it reads `lib/store.ts` itself, exactly
> like `HouseView` already does.
>
> Do not add new store fields, do not touch `lib/store.ts`, `lib/units.ts` or `lib/i18n.ts`, and do
> not edit any file under `components/**`. This is a three-line change in one file: one new import,
> one new wrapper `<div>`, one new `<DayNightAnimation />` element.

**Files you may touch.** `apps/web/app/app-shell.tsx` only.
**Files you may NOT touch.** `apps/web/app/page.tsx`, `apps/web/lib/store.ts`,
`apps/web/lib/units.ts`, `apps/web/lib/i18n.ts`, `apps/web/app/globals.css`, anything under
`apps/web/components/**`.

**Subagent guidance.** Single agent. Three-line wiring change in one file.

**ACCEPTANCE TESTS — the task is NOT done until every one passes:**

1. `slot-house`'s `<main>` now contains both `data-testid="daynight"` and
   `data-testid="house-view"`, wrapped in a `position: relative` element, with `daynight` appearing
   **before** `house-view` in DOM order (so it paints behind). Paste the rendered order and the
   wrapper's computed/inline `position` value.
2. `git diff` touches only `apps/web/app/app-shell.tsx`; `lib/store.ts`, `lib/units.ts`,
   `lib/i18n.ts` and everything under `components/**` are byte-identical to before. Paste
   `git status --short`.
3. `npx tsc --noEmit -p apps/web/tsconfig.json` exits with 0 new errors versus the pre-task
   baseline. Paste the before/after error counts.
4. `npm run build --workspace apps/web` exits 0 (the pre-existing, unrelated `topLevelAwait`
   warning from `@shelter/engine/dist/serialise.js` is expected and not a regression — paste the
   full warning list and confirm nothing new).
5. `npx vitest run apps/web/components/house` shows the same pass count T-46's and T-53's own
   Evidence blocks recorded (no file under `components/house/**` touched by this task).
6. **Alignment, proven through the real shell for the first time (not just each task's own isolated
   math):** `DayNightAnimation`'s rendered `<svg data-testid="daynight-scene">` `viewBox` attribute
   is byte-identical to `HouseView`'s own rendered `<svg>` `viewBox` attribute, for the same store
   state. Paste both strings.
7. `appState.result === null` (a hard reload before any simulation resolves) does not crash either
   component — `DayNightAnimation` reads only `request`/`status`/`scrubberHour`, never `result`, so
   it must render identically regardless. Paste the render result with `result` forced null.
8. With `store.status = 'running'`, `DayNightAnimation`'s progress ring and looping behaviour
   (T-53 acceptance tests 6/7/9) still work unchanged when mounted as a sibling of the real
   `HouseView` rather than in isolation — paste one observed progress-ring sequence through the
   real shell.

**Evidence (fill this in when done — numbers, not adjectives):**

```
Environment: npm install (442 packages), npx prisma generate --schema apps/web/prisma/schema.prisma
(Generated Prisma Client v6.19.3), then npm run build --workspace @shelter/engine / @shelter/data /
@shelter/optimise (all tsc -b, exit 0, dist/ produced) before tsc/build.

Change made (exactly the 3-line diff the PROMPT specifies), apps/web/app/app-shell.tsx:
  import { DayNightAnimation } from '../components/house/daynight';
  ...
  <div style={{ position: 'relative' }}>
    <DayNightAnimation />
    <HouseView />
  </div>

Verification method: a temporary file apps/web/app/__t75-verify.test.tsx (react-dom/server
renderToStaticMarkup against the real store + @shelter/engine simulate() on the bundled Leh preset,
same convention as T-46/T-53's own test files -- no jsdom/@testing-library in this project, see
CONTRACTS.md §7.13). Deleted before the final commit; git status --short below shows it is gone.

1. Rendered <AppShell> markup, real Leh preset: daynightIdx=7991, houseViewIdx=12888,
   wrapperIdx (position:relative wrapper immediately preceding daynight)=7943.
   order: daynightIdx(7991) < houseViewIdx(12888) -> true. Wrapper's inline style found via
   `style="position:relative"` at index 7943, before daynight -> the div wraps both.
   PASS.

2. `git status --short` after deleting the temp verify file:
     M apps/web/app/app-shell.tsx
   Only app-shell.tsx touched. `git diff --stat` confirms 1 file changed. lib/store.ts, lib/units.ts,
   lib/i18n.ts and everything under components/** are untouched (git diff on those paths is empty).
   PASS.

3. `npx tsc --noEmit -p apps/web/tsconfig.json`: BEFORE the edit = 0 errors (grep -c 'error TS' = 0,
   exit 0). AFTER the edit = 0 errors (exit 0). 0 new errors. PASS.

4. `npm run build --workspace apps/web` (next build --webpack) exits 0. Full warning list produced:
     "The generated code contains 'async/await' because this module is using \"topLevelAwait\"."
     (from ../../node_modules/@shelter/engine/dist/serialise.js, import trace through ./lib/units.ts
     -> ./app/app-shell.tsx) -- this is the pre-existing, documented warning; nothing else appeared.
   Route summary printed normally (/ , /api/designs, /api/materials, /api/simulate, /api/weather,
   /_not-found). PASS.

5. `npx vitest run apps/web/components/house`: 2 test files passed (2), 22 tests passed (22), both
   BEFORE and AFTER this task's edit (identical counts; no file under components/house/** was
   touched). PASS.

6. Byte-identical viewBox, same store state (real Leh preset, real simulate() result):
     DayNightAnimation's <svg data-testid="daynight-scene"> viewBox = "-5.98 -6.47 11.96 10.35"
     HouseView's            <svg>                          viewBox = "-5.98 -6.47 11.96 10.35"
   Identical strings. PASS.

7. `actions.setResult(null)` then re-hydrated with result: null; rendering
   <div style={position:relative}><DayNightAnimation/><HouseView/></div> did not throw (verified
   with expect(...).not.toThrow()) and produced markup (length 7266 chars) containing both
   data-testid="daynight" and data-testid="house-view". No crash with appState.result === null.
   PASS.

8. `actions.setStatus('running')`; progress ring sequence observed through the real shell
   (DayNightAnimation mounted as a sibling of the real HouseView, exactly as wired in app-shell.tsx):
     setProgress({done:0,total:10})  -> ring text "0/10"
     setProgress({done:3,total:10})  -> ring text "3/10"
     setProgress({done:10,total:10}) -> ring text "10/10"
   Sequence == ['0/10','3/10','10/10']. Matches T-53's own test-6 shape, now proven through the real
   shell rather than DayNightAnimation in isolation. PASS.

CONDITIONS CHECKLIST: 1 PASS / 2 PASS / 3 PASS / 4 PASS / 5 PASS / 6 PASS / 7 PASS / 8 PASS -- 8/8.

Commands to reproduce, from a fresh worktree:
  npm install
  npx prisma generate --schema apps/web/prisma/schema.prisma
  npm run build --workspace @shelter/engine
  npm run build --workspace @shelter/data
  npm run build --workspace @shelter/optimise
  npx tsc --noEmit -p apps/web/tsconfig.json
  npm run build --workspace apps/web
  npx vitest run apps/web/components/house

Decisions/notes for a zero-context successor:
- Wrapper div uses an inline `style={{ position: 'relative' }}`, no new CSS class, matching the
  PROMPT exactly and not touching globals.css (off the allow-list anyway).
- Import path is the barrel `../components/house/daynight` (its index.ts already exports
  DayNightAnimation), not a deep import into DayNightAnimation.tsx.
- No gotchas hit: DayNightAnimation and HouseView already shared the identical
  boundingBox(building)/pad=1 viewBox formula (T-53 built it that way on purpose), so alignment
  fell out for free once stacked -- no adjustment needed in either component (neither was touched;
  both are outside this task's allow-list regardless).
- The temporary verification file (apps/web/app/__t75-verify.test.tsx) was created, run, and then
  deleted -- it is NOT part of the final diff. `git status --short` above is the proof.
```

**Completed by:** T-75 subagent (session 10) **Date:** 2026-09-21
