### [ ] T-80 — Wire real connectivity detection into `dispatchSimulation()` so `store.online` is honest

**Area:** J — Delivery · **Status:** NOT STARTED · **Est:** 1.5 h
**Depends on:** T-66 · **Conflicts with:** T-36 (store.ts — check it is not claimed)

**Why this exists.** T-66 built and self-tested a correct offline story (service worker,
manifest, static export, the OfflineBanner's exact text) but found and honestly reported that
`store.online` never reflects real connectivity in the live app: `dispatchSimulation()`
(`apps/web/lib/store.ts`, the ONE place `request` mutations turn into a simulation run) calls
`simulate()` synchronously in-process and never checks connectivity at all, so `online` stays at
its `true` default forever, live. This is T-66 acceptance test 10's still-open live-app half
(the static-export half already passes — `build-static.mjs` forces `setOnline(false)` on mount).
T-77 (DONE) closed the sibling gap — service-worker registration and the manifest link — but
explicitly did not touch `lib/store.ts`. T-78 (DONE) closed a separate, unrelated gap (a tsconfig
type-check issue). This task closes the one piece neither of those did.

**PROMPT — paste this to start the task:**
> Make `store.online` reflect real connectivity, without making any demo-path feature depend on
> a server. The core simulation (`simulate()`, called synchronously by `dispatchSimulation()`)
> must keep running locally and unconditionally exactly as it does today — connectivity only
> gates the banner/AI-advice-availability signal, never the computation itself
> (`log/AREA-J/T-66.md`'s own brief: "no demo-path feature may require an API route").
>
> **Hard constraint, read this before you start:** `apps/web/lib/workerClient.ts` already imports
> `actions` from `./store.js` (line 40). `lib/store.ts` therefore CANNOT import anything from
> `workerClient.ts` — that would be a circular import. Do not reuse `isServerReachable()` from
> `workerClient.ts` by importing it. Either inline an equivalent, minimal check directly in
> `store.ts` (native `navigator.onLine`, and/or a `window`-level `'online'`/`'offline'` event
> listener — both are native platform features, no new dependency, no import needed), or find
> another way that does not create the cycle. This module also runs in the SERVER bundle
> (`app/page.tsx` imports it for `DEFAULT_LOCATION`), so any `window`/`navigator` access must be
> guarded with `typeof window !== 'undefined'` / `typeof navigator !== 'undefined'` — the existing
> code in this file already has zero such guards today because it never touches either; follow the
> guard style `workerClient.ts`'s own `isServerReachable()` uses as your reference (read it, don't
> copy-import it).
>
> Wire this into `dispatchSimulation()` (and/or `hydrateStore()`/a one-time setup, if a live
> `online`/`offline` event listener is part of your approach) so that `store.online` becomes
> `false` when the browser is actually offline and `true` again when it comes back — verified by
> your own test, not just by inspection.
>
> No new npm dependency (`log/contracts/00-core.md` §7.13 is closed). No UI changes. Do not touch
> the OfflineBanner or any component — they already read `store.online` correctly (T-52's own
> `apps/web/components/meta/locale/locale.test.tsx` test 10 already proves this with a manual
> `actions.setOnline(false)` call; your job is only to make that call happen for real, automatically).

**Files you may touch.** `apps/web/lib/store.ts` only, plus a new or existing test file that
exercises it (there is currently no dedicated `store.test.ts` — `apps/web/components/inputs/
inputs.test.ts` imports `dispatchSimulation`'s effects via `__debugDispatchCount`/`actions`/
`getStoreState`/`hydrateStore` from `../../lib/store` for its own debounce test; either add
`apps/web/lib/store.test.ts` as a new file, or add cases to an existing test file that already
imports from `lib/store` — your choice, document which in the Evidence block).
**Files you may NOT touch.** `lib/workerClient.ts`, `app/layout.tsx`, `app/app-shell.tsx`,
`app/page.tsx`, any component directory, `lib/units.ts`, `lib/i18n.ts`.

**Subagent guidance.** Single agent. This is a small, surgical addition inside one file (same
shape as T-76: a small wiring change plus a test), not a rewrite of `dispatchSimulation()`.

**Test-environment note.** This repo's vitest runs in the default **node** environment — no
jsdom (`vitest.config.ts` has no `environment: 'jsdom'`, and none is on the approved dependency
list). `navigator`/`window` do not exist unless a test sets them on `globalThis` itself.
`apps/web/test/worker.test.ts` already does exactly this for `globalThis.fetch` to test
`isServerReachable()` (e.g. `globalThis.fetch = (() => Promise.reject(new Error('offline'))) as
typeof fetch;` — grep that file for the full pattern) — reuse the same technique for whatever
global(s) your implementation reads (`navigator`, `window`, or both).

**GOTCHA — read before running `npx vitest run` (learned the hard way merging T-79):** a fresh
worktree has no database at all. 8 test files (33 tests) genuinely fail with a Prisma
"protocol must be postgresql://" error if you just run `npx vitest run` — this is NOT a
regression from your change, it's a missing setup step, but you must still do the setup so your
acceptance test 4's counts are the real, fully-green baseline, not a false-negative one. Run, from
`apps/web/`:
```
DATABASE_PROVIDER=sqlite DATABASE_URL="file:./dev.db" npm run db:migrate
DATABASE_PROVIDER=sqlite DATABASE_URL="file:./dev.db" npm run db:seed
```
then run the suite itself with the same two env vars set:
```
DATABASE_PROVIDER=sqlite DATABASE_URL="file:./dev.db" npx vitest run
```
`npx prisma generate --schema apps/web/prisma/schema.prisma` (the command named in some older task
briefs) is NOT enough by itself and will generate a client hardcoded to `postgresql`, causing the
exact error above — `db:migrate`/`db:seed` run `prisma/generate-schema.mjs` first, which rewrites
the provider from `DATABASE_PROVIDER`. The full green baseline is **45 files, 492 passed, 10
skipped** — that is the number your acceptance test 4 compares against, not whatever a
DB-less run shows.

**ACCEPTANCE TESTS — the task is NOT done until every one passes:**
1. A test that fakes "offline" (however your implementation detects it — e.g. setting
   `globalThis.navigator = { onLine: false } as Navigator` and/or dispatching a fake `'offline'`
   event on a faked `globalThis.window`) and then triggers a dispatch (mutate `request` via
   `actions.setRequest`/whichever public action causes `dispatchSimulation()` to run, and await
   the debounce) shows `getStoreState().online === false` afterward. Paste the test name and the
   assertion result.
2. The inverse: faking "online" again afterward (or starting online) shows `getStoreState().online
   === true`. Paste the test name and result.
3. `simulate()`'s result is still computed and stored (`getStoreState().result` is non-null and
   correct) in BOTH the online and offline cases from tests 1–2 — connectivity must never block or
   change the local computation. Paste both results' key figure (e.g. `tempAt0600`) and confirm
   they match (same request in both cases).
4. `npx vitest run` passes at least as many tests as the pre-task baseline, with zero regressions
   — in particular `apps/web/components/inputs/inputs.test.ts` test 5 (the debounce test:
   "changing wall material triggers exactly one re-simulation after the debounce") and
   `apps/web/components/meta/locale/locale.test.tsx` test 10 (the offline-banner text test) must
   both still pass unchanged. Paste: total test files, total tests, pass count, skip count.
5. `npx tsc --noEmit -p apps/web/tsconfig.json` exits 0. Paste the exit code.
6. `git diff --stat HEAD~1 HEAD` shows only `apps/web/lib/store.ts` and your one test file changed
   (plus this task's own log file and `LOG.md`). Paste the output.
7. Confirm no import of `./workerClient` (or `workerClient.js`) was added to `store.ts` —
   `grep -n "workerClient" apps/web/lib/store.ts` must return nothing. Paste the (empty) output.

**Evidence (fill this in when done — numbers, not adjectives):**
```
(empty — task not yet started)
```

**Completed by:** — **Date:** —
