id: T-28
title: Presets: the app opens on an interesting result
status: DONE
owner: claude (T-28 subagent)
claimed: 2026-09-16T06:20:14Z
completed: 2026-09-16

## Files created or changed

- `packages/data/src/presets.ts` (new) -- `PRESETS: readonly Preset[]` (6 presets) and `presetById(id)`.
- `packages/data/test/presets.test.ts` (new) -- 12 acceptance tests, numbered to match
  `log/AREA-C-data-layer.md`'s T-28 entry.
- `log/AREA-C-data-layer.md` -- T-28 heading flipped `[~]` -> `[x]`, Evidence block filled in,
  Completed-by/Date filled in.
- `LOG.md` -- §5 dashboard: Area C row 4/5 -> 5/5, TOTAL 26/69 -> 27/69, T-28 row `[~]` -> `[x]`.

## Verification results

See `log/AREA-C-data-layer.md`'s T-28 Evidence block for all 12 tests with measured numbers
(residuals, tempAt0600 values, peakToPeakSwing values, etc.) -- not duplicated here to avoid the
numbers drifting out of sync between two files. Summary: all 12 acceptance tests pass;
`npx vitest run` from the worktree root -> 17 files, 214 passed / 10 skipped, 0 failed;
`npx tsc -b packages/data` and `npx tsc -b packages/engine` both exit 0 clean.

## Deviations and notes for downstream tasks

**For T-56 (the design-search/optimiser) to close:** preset 5 (`optimisedPassivePlaceholder` in
`packages/data/src/presets.ts`) is a hand-authored, reasonably well-insulated stand-in, NOT a sweep
result -- `approximations: ['placeholder -- to be replaced by a real sweep winner, see T-56']`, per
the PROMPT's own instruction and `AUDIT.md` C-16 (never hand-author an "optimised" preset and present
it as one). When T-56 ships: run a real `SweepRequest` over this same base building (or a fresh one),
take the Pareto winner, and replace `PRESET_OPTIMISED_PLACEHOLDER` in presets.ts with the real result
-- paste the winner's actual `SweepRequest` and its date in a comment, remove the placeholder
`approximations` entry, and update `presets.test.ts`'s acceptance test 8 accordingly (it currently
only checks for the placeholder marker).

**Two findings reported upward, not fixed here (out of this task's `packages/data/**`-only file
scope):**

1. **Engine bug** in `packages/engine/src/solar/transposition.ts`'s HDKR branch: `Rb = cosTheta /
   sun.cosZenith` has a lower floor (`> 1e-6`) but no upper clamp. At Leh's latitude in January, near
   sunrise (`sunPosition(34.15, 77.58, 82.5, 1, 7.5).cosZenith` = 0.0014), `Rb` reaches several
   hundred and the HDKR diffuse term spikes to an unphysical multi-kW/m^2 for that hour --
   `EngineError('SOLVER_DIVERGED', ...)` on every Ladakh preset tried with the `DEFAULT_SIM_OPTIONS`
   default `skyModel: 'hdkr'`, reproduced on five structurally unrelated envelopes. Worked around here
   (and already, silently, in `packages/data/test/tmy.test.ts`'s own T-27 fixture) by using
   `skyModel: 'isotropic'` instead -- see the code comment above `OPTIONS` in `presets.ts` for the
   full bisection. Needs a real fix in Area B (a clamp on `Rb`, or gating the HDKR branch on a minimum
   solar altitude) before `'hdkr'` is safe to use as the default for any high-latitude-winter site,
   Leh included.
2. **Doc/code discrepancy**: `CONTRACTS.md` 7.5 says `Surface.area` is "NET, not gross ... do not
   subtract them again", but `packages/engine/src/solve/assemble.ts` (`opaqueArea = s.area -
   windowArea`) and every passing fixture (including this task's own presets and T-27's own test 11)
   actually treat it as GROSS (window area included, then subtracted). Followed the working code here;
   `CONTRACTS.md` 7.5's wording looks like it needs a correction, not a re-litigation of the actual
   implementation.

## Blocked by

(not applicable -- status is DONE)
