id: T-23
title: Validation Test 5 against NOAA, and print every measured pair
status: CLAIMED          # CLAIMED | BLOCKED | DONE | ABANDONED
owner: orch-T-23
claimed: 2026-09-15
completed:

## Files created or changed
- packages/engine/test/gate.test.ts, packages/engine/test/mesh.test.ts,
  packages/engine/test/integrator.test.ts -- Piece 2, done: added a `reportComparison()` helper
  beside every existing analytical `expect(...)`, printing `TEST<n> <case>: measured <x>,
  analytical <y>, deviation <z>%` and appending one row to
  packages/engine/test/output/validation-numbers.csv (columns test,case,measured,analytical,
  deviation_pct,tolerance,pass). No assertion or tolerance touched -- pure side effect, added
  beside the pre-existing `expect(...)` lines, never replacing them.
- packages/engine/test/output/validation-numbers.csv -- generated by the above, committed.
- packages/engine/test/validation-noaa.test.ts -- Piece 1, NOT YET DONE, in progress.

## Verification results
(Piece 2 -- acceptance tests 7-12 in log/AREA-B-engine.md's T-23 entry)
7. `npx vitest run` prints TEST1/TEST2/TEST3/TEST4/TEST6/TEST7/TEST8 `measured ..., analytical ...,
   deviation ...%` lines: PASS, confirmed in stdout.
8. packages/engine/test/output/validation-numbers.csv exists, header has the 7 named columns
   (test,case,measured,analytical,deviation_pct,tolerance,pass), 105 data rows: PASS.
9. Every `pass` column value is `true` (checked all 105 rows): PASS.
10. Every deviation_pct is within its row's tolerance -- this is exactly what the `pass` column
    encodes, computed in the same TS expression as the real `expect(...)` beside it: PASS.
11. `git diff packages/engine/test/gate.test.ts | grep -c "^-.*expect"` = 0;  same for mesh.test.ts
    and integrator.test.ts: PASS, no assertion removed or altered.
12. `npx vitest run` exits 0, 127 tests passing (> 65 required): PASS.

(Piece 1 -- acceptance tests 1-6 -- pending, see Blocked by / in-progress notes)

## Deviations and notes for downstream tasks
- Piece 2 was found already implemented, uncommitted, at the start of this session (matching this
  task's own Piece 2 prompt exactly) -- verified against all six of its acceptance tests before
  being folded into this claim. No changes were made to it beyond verification.
- T-63 (VALIDATION.md) can lift packages/engine/test/output/validation-numbers.csv directly, per
  this task's own note.

## Blocked by
(not blocked -- Piece 1 in progress by a dispatched subagent)
