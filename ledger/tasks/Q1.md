# Q1 — E2E + visual QA
Status: TODO      Depends on: all

## Goal
Prove the whole flow works and looks like a serious engineering product. Findings are routed to the owning task. Do
not fix them here.

## Read
`ledger/PLAN.md` · `STUDIO.md` · `apps/studio-server/API.md`

## May touch
`apps/studio/e2e/**` (a playwright-core script using system Chrome `/usr/bin/google-chrome`; playwright-core is
installed in the scratchpad, not the repo). A findings report goes in this file's Evidence block.

## Conditions
1. E2E under `timeout 180`: register → new design → change width (the viewer bounding box grows) → preview completes
   → save → run saved → second design → compare shows both → logout → `/app` redirects to login. 0 console errors.
2. Visual QA of every page at 1440/390 px in light and dark: spacing, alignment, hierarchy, empty/loading/error states,
   chart legibility, viewport composition, and the banned-pattern list. Each finding lists page, screenshot, severity
   and owner task.
3. All suites are green: studio-server, studio, old server/engine/data. Build is clean. The frozen-app guard is empty.

## Evidence
