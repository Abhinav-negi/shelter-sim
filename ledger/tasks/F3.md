# F3 — Studio page: controls, live preview, results
Status: IN-PROGRESS (WIP committed, agent hit session limit)      Depends on: F1, F1b, F2, P2 (all merged)

## Goal
The core design experience: controls → store → 3D model, with a debounced preview simulation and results that
explain the outcome.

## Read
`ledger/PLAN.md` (§Client, §Results) · `apps/studio-server/API.md` · `apps/studio/src/{design,api,viewer}/**` ·
`apps/client/src/features/results/{explain.ts,labels.ts}` (port the logic, not the UI)

## Building blocks that already exist (use them, don't rebuild)
- `src/viewer/ShelterViewer.tsx`: `<ShelterViewer design options hour />` (prop-driven, lazy-load it; three is a
  ~950 kB chunk). F2 also exposes the sun elevation for a "sun below horizon" note.
- `src/design/useOptions.ts`: `useOptions()`, one shared cached `/api/options` fetch.
- `src/design/store.ts`: `useShelterDesign` (zustand) with typed setters per control group plus `loadDesign`/`reset`/`dirty`.
- `src/api/{simulate,designs,locations}.ts`: `previewSimulation`, `createDesign`/`updateDesign`/`getDesign`,
  `runSimulation`/`listSimulations`/`getSimulation`, `searchLocations`. Types come from `@shelter/studio-server`.
- UI primitives `src/components/ui/{Button,Input,Slider,Select,Segmented,FieldRow}` and the tokens in `src/styles/tokens.css`.

## May touch / must not touch
May: `apps/studio/src/{controls,results,routes/Studio*}/**`. Must not: the viewer internals, the store API, server,
frozen. **No npm install.**

## Conditions
1. Control groups: Environment (preset/custom location via search, date), Geometry, Materials (material + thickness
   per surface), Openings (WWR per facade, glazing, shutters), Orientation (dial), Advanced (occupancy). Ranges come
   from `/api/options`.
2. Each change updates the 3D model immediately and triggers a preview 400 ms after the last change. Stale responses
   are discarded (AbortController).
3. The status shows the real stages, not timers. Errors say what happened and offer a retry. Custom-location weather
   provenance notes are visible.
4. Results: indoor vs outdoor chart (°C, thermal colours), heat-flow pathway breakdown (daily kWh), the engine KPIs
   listed in PLAN.md, and "What happened / Why" from the ported explain logic. No invented metrics.
5. Save design / Save run (P2 routes) persist the design and the run.
6. Screenshots at 1440 and 390 px, light and dark. No console errors.

## Rules
Do not expose every engine variable. Advanced stays collapsed. Units are always shown in mono.

## Evidence

## Resume notes (orchestrator, 2026-09-26)
WIP commit `76665da` on `task/f3` in `../wt-f3`. Present: `src/controls/{ControlsPanel,Section,SliderField,
Environment,Geometry,Materials,Openings,Orientation,Advanced}Section.tsx`, `LocationSearch.tsx`, `OrientationDial.tsx`,
`useDebouncedRequest.ts(+test)`; `src/results/{ResultsPanel,TemperatureChart,HeatFlowChart,Kpis}.tsx`,
`explain.ts(+test)`, `format.ts(+test)`, `labels.ts`, `types.ts`; `routes/Studio.tsx` modified. Build clean, 56/56
tests. The agent's last action: "wrap ResultsPanel usage in Suspense" (lazy results chunk). Not done: confirm the
Studio page wiring is complete, then conditions 2–6 flow + visual checks (scratch Mongo server recipe is in the
original brief: MongoMemoryServer + `connectDb` + `buildApp({jwtSecret:'scratch'}).listen({port:4106})`, vite
--port 5278), then Evidence.
